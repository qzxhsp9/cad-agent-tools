# 立即执行：发布并接入 AIDT

当前交付物已经是标准 Python 包，但我没有你的 PyPI/GitHub 凭据，因此没有替你上传到公网仓库。

## 最快方案：先推 GitHub，立即在 AIDT 添加

在项目根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\publish_github.ps1
```

脚本默认创建或更新：

```text
https://github.com/qzxhsp9/cad-agent-tools
```

然后在 AIDT 中粘贴：

```json
{
  "mcpServers": {
    "cad-agent-tools": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/qzxhsp9/cad-agent-tools.git@main",
        "cad-agent-tools"
      ]
    }
  }
}
```

## 正式方案：发布 PyPI

```powershell
$env:UV_PUBLISH_TOKEN = "你的 PyPI token"
uv publish
Remove-Item Env:UV_PUBLISH_TOKEN
```

发布成功后，AIDT 改成最简配置：

```json
{
  "mcpServers": {
    "cad-agent-tools": {
      "command": "uvx",
      "args": ["cad-agent-tools@0.5.0"]
    }
  }
}
```

## 前提

AIDT 的 MCP Runner 必须：

- 能找到 `uvx`；
- 能访问 PyPI、公司私有包仓库或 GitHub；
- 能访问用户上传文件被物化后的本地路径。

此方案没有 HTTP 服务、端口、常驻进程、Python 绝对路径、venv 路径或固定 CAD 数据目录。
